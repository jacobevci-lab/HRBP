import { DataClassification, PlatformRole, ServiceVisibility } from "@prisma/client";
import { db } from "@/lib/db";
import { can, forbidden } from "@/lib/authorization";
import { appendAudit } from "@/lib/audit";
import { getRequestContext, unauthorized } from "@/lib/request-context";
const selfService=new Set<PlatformRole>([PlatformRole.EMPLOYEE,PlatformRole.MANAGER]);
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const ctx=getRequestContext(request);if(!ctx)return unauthorized();if(!can(ctx,"hr-service:read"))return forbidden();const {id}=await params;const ticket=await db.hRServiceRequest.findFirst({where:{id,tenantId:ctx.tenantId,...(selfService.has(ctx.role)?{requestorId:ctx.actorId}:{})},select:{id:true}});if(!ticket)return Response.json({error:"Request not found."},{status:404});const data=await db.hRServiceComment.findMany({where:{tenantId:ctx.tenantId,requestId:id,...(selfService.has(ctx.role)?{visibility:ServiceVisibility.REQUESTOR}:{})},orderBy:{createdAt:"asc"}});return Response.json({data})}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const ctx=getRequestContext(request);if(!ctx)return unauthorized();if(!can(ctx,"hr-service:write"))return forbidden();const {id}=await params;const body=await request.json() as {body?:string;visibility?:ServiceVisibility};const text=body.body?.trim();if(!text)return Response.json({error:"body is required."},{status:400});const ticket=await db.hRServiceRequest.findFirst({where:{id,tenantId:ctx.tenantId,...(selfService.has(ctx.role)?{requestorId:ctx.actorId}:{})},select:{id:true}});if(!ticket)return Response.json({error:"Request not found."},{status:404});const visibility=selfService.has(ctx.role)?ServiceVisibility.REQUESTOR:(body.visibility&&Object.values(ServiceVisibility).includes(body.visibility)?body.visibility:ServiceVisibility.REQUESTOR);const data=await db.$transaction(async tx=>{const comment=await tx.hRServiceComment.create({data:{tenantId:ctx.tenantId,requestId:id,authorId:ctx.actorId,body:text,visibility}});await appendAudit(tx,ctx,{action:"hr-service.comment-added",resourceType:"HRServiceComment",resourceId:comment.id,classification:DataClassification.CONFIDENTIAL});return comment});return Response.json({data},{status:201})}
