import { DataClassification, PolicyAssignmentStatus, PolicyStatus } from "@prisma/client";
import { db } from "@/lib/db";
import { can, forbidden } from "@/lib/authorization";
import { appendAudit } from "@/lib/audit";
import { getRequestContext, unauthorized } from "@/lib/request-context";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const ctx=getRequestContext(request);if(!ctx)return unauthorized();if(!can(ctx,"policies:write"))return forbidden();const {id}=await params;const body=await request.json() as {employmentIds?:string[];dueAt?:string};const ids=[...new Set((body.employmentIds??[]).filter(Boolean))];if(!ids.length)return Response.json({error:"employmentIds are required."},{status:400});const data=await db.$transaction(async tx=>{const policy=await tx.policyRecord.findFirst({where:{id,tenantId:ctx.tenantId,status:PolicyStatus.PUBLISHED},select:{id:true}});if(!policy)throw new Error("NOT_FOUND");const valid=await tx.employment.findMany({where:{tenantId:ctx.tenantId,id:{in:ids}},select:{id:true}});if(valid.length!==ids.length)throw new Error("EMPLOYMENT");await tx.policyAssignment.createMany({data:ids.map(employmentId=>({tenantId:ctx.tenantId,policyId:id,employmentId,dueAt:body.dueAt?new Date(body.dueAt):undefined,status:PolicyAssignmentStatus.PENDING})),skipDuplicates:true});await appendAudit(tx,ctx,{action:"policy.assignments-created",resourceType:"PolicyRecord",resourceId:id,classification:DataClassification.INTERNAL,purpose:`Assigned to ${ids.length} employment records`});return {assigned:ids.length}}).catch(e=>e instanceof Error&&["NOT_FOUND","EMPLOYMENT"].includes(e.message)?e.message:Promise.reject(e));if(data==="NOT_FOUND")return Response.json({error:"Published policy not found."},{status:404});if(data==="EMPLOYMENT")return Response.json({error:"One or more employment records are outside this tenant."},{status:400});return Response.json({data},{status:201})}
