import { DataClassification, DocumentStatus, SignatureEnvelopeStatus } from "@prisma/client";
import { db } from "@/lib/db";
import { can, forbidden } from "@/lib/authorization";
import { appendAudit } from "@/lib/audit";
import { getRequestContext, unauthorized } from "@/lib/request-context";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const ctx=getRequestContext(request);if(!ctx)return unauthorized();if(!can(ctx,"documents:read"))return forbidden();const {id}=await params;const doc=await db.documentRecord.findFirst({where:{id,tenantId:ctx.tenantId,status:{not:DocumentStatus.DELETED},classification:{not:DataClassification.HIGHLY_RESTRICTED}},select:{id:true}});if(!doc)return Response.json({error:"Document not found or restricted."},{status:404});return Response.json({data:await db.signatureEnvelope.findMany({where:{tenantId:ctx.tenantId,documentId:id},include:{participants:true,events:{orderBy:{occurredAt:"asc"}}},orderBy:{createdAt:"desc"}})})}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const ctx=getRequestContext(request);if(!ctx)return unauthorized();if(!can(ctx,"documents:sign"))return forbidden();const {id}=await params;const body=await request.json() as {title?:string;expiresAt?:string;participants?:Array<{employmentId?:string;email?:string;signingOrder?:number}>};const title=body.title?.trim();if(!title||!body.participants?.length)return Response.json({error:"title and participants are required."},{status:400});const data=await db.$transaction(async tx=>{const doc=await tx.documentRecord.findFirst({where:{id,tenantId:ctx.tenantId,status:{not:DocumentStatus.DELETED},classification:{not:DataClassification.HIGHLY_RESTRICTED}}});if(!doc)throw new Error("NOT_FOUND");const envelope=await tx.signatureEnvelope.create({data:{tenantId:ctx.tenantId,documentId:id,title,status:SignatureEnvelopeStatus.SENT,createdById:ctx.actorId,expiresAt:body.expiresAt?new Date(body.expiresAt):undefined,participants:{create:body.participants!.map((p,i)=>({tenantId:ctx.tenantId,employmentId:p.employmentId,email:p.email,signingOrder:p.signingOrder??i+1}))},events:{create:{tenantId:ctx.tenantId,eventType:"envelope.sent",actorId:ctx.actorId,ipAddress:ctx.ipAddress}}},include:{participants:true}});await appendAudit(tx,ctx,{action:"document.signature-envelope-sent",resourceType:"SignatureEnvelope",resourceId:envelope.id,classification:doc.classification});return envelope}).catch(e=>e instanceof Error&&e.message==="NOT_FOUND"?null:Promise.reject(e));if(!data)return Response.json({error:"Document not found or restricted."},{status:404});return Response.json({data},{status:201})}
